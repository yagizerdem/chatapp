class ProfileUpasertModel {
  constructor({ address, biography, profileBase64 }) {
    this.address = address;
    this.biography = biography;
    this.profileBase64 = profileBase64;
  }
}

export default ProfileUpasertModel;
